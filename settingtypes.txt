geminti.enabled (Enable Geminti) bool true
geminti.api_key (Gemini API key) string
geminti.name (Geminti displayed name in the chat) string Geminti
geminti.name_prefix (Geminti displayed name prefix) string [AI]
geminti.model (Geminti model) string gemini-2.5-flash-lite
geminti.prefix (Geminti chat prefix) string ;
geminti.color (Geminti responses color) string #aef
geminti.newlines (Allow newlines in Geminti responses) bool false
geminti.max_errors (Maximum errors count before context reset) int 3
geminti.system_prompt (Geminti system prompt) string You are in a multiuser chat. Messages follow the pattern '<username> message', where <username> is the sender's name and 'message' is their content. Do not use <username> prefix in your messages.
